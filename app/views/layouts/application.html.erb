<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Volunteers App" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <link rel="manifest" href="/manifest.json">
  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
<nav class="bg-blue-600 text-white p-4 shadow-md">
  <div class="container mx-auto flex justify-between items-center">
    <%= link_to "Volunteers App", root_path, class: "text-xl font-bold" %>
    <div class="flex space-x-4 items-center">
      <% if user_signed_in? %>
        <% unread_count = current_user.notifications.where(read: false).count %>
        <div class="relative">
          <%= link_to notifications_path, class: "text-white hover:text-gray-200 text-xl" do %>
            🔔
          <% end %>
          <% if unread_count > 0 %>
            <span class="absolute -top-1 -right-1 bg-red-600 text-white text-xs rounded-full px-1.5">
              <%= unread_count %>
            </span>
          <% end %>
        </div>

        <%= link_to "🏠", root_path, class: "text-white hover:text-gray-200 px-4 text-lg" %>
        <%= link_to "Dashboard", dashboard_path, class: "text-white hover:text-blue-200 transition" %>
        <%= link_to "My Profile", profile_path, class: "text-white hover:text-blue-200 transition" %>
        <%= link_to "Запити", requests_path, class: "text-white hover:text-blue-200 transition" %>
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete,
                      class: "bg-white text-blue-600 px-4 py-2 rounded hover:bg-gray-100 transition" %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path,
                    class: "bg-white text-blue-600 px-4 py-2 rounded hover:bg-gray-100 transition" %>
        <%= link_to "Sign Up", new_user_registration_path,
                    class: "bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-700 transition" %>
      <% end %>
    </div>
  </div>
</nav>

<main class="container mx-auto p-4">
  <% if notice %>
    <p class="bg-green-100 text-green-800 p-3 rounded mb-4"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="bg-red-100 text-red-800 p-3 rounded mb-4"><%= alert %></p>
  <% end %>

  <%= yield %>
</main>
</body>
</html>
