<h1 class="text-2xl font-bold mb-2"><%= @request.title %></h1>
<p><%= @request.description %></p>
<p><strong>Категорія:</strong> <%= @request.category %></p>
<p><strong>Місцезнаходження:</strong>
  <%= @request.location || "Не вказано" %>
  <% if current_user == @request.user && @request.location.blank? %>
    <span class="text-sm text-red-600">
      (Будь ласка, <%= link_to "додайте місцезнаходження", edit_profile_path %> у вашому профілі)
    </span>
  <% end %>
</p>

<%# Кнопка "Завершити запит" %>
<% if (current_user.role == "волонтер" || current_user == @request.user) && !@request.completed? %>
  <%= button_to "✅ Завершити запит",
                complete_request_path(@request),
                method: :patch,
                class: "mt-4 bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700" %>
<% end %>

<%# Кнопка "Змінити запит" %>
<% if current_user == @request.user && !@request.completed? %>
  <%= link_to "✏️ Змінити запит", edit_request_path(@request),
              class: "inline-block mt-4 bg-gray-100 border border-gray-400 text-gray-800 px-4 py-2 rounded hover:bg-gray-200" %>
<% end %>

<p class="mt-2">
  <strong>Статус:</strong>
  <span class="inline-block px-3 py-1 rounded-full text-sm font-semibold
    <%= case @request.status
        when 'Потрібна допомога' then 'bg-yellow-100 text-yellow-800'
        when 'У процесі' then 'bg-blue-100 text-blue-800'
        when 'Завершено' then 'bg-green-100 text-green-800'
        else 'bg-gray-200 text-gray-800'
        end %>">
    <%= @request.status %>
  </span>
</p>
<%#Chat button — visible to request author and the volunteer who responded %>
<% if current_user == @request.user || @request.responses.exists?(user_id: current_user.id) %>
  <%= link_to "💬 Перейти до чату", request_messages_path(@request),
              class: "inline-block mt-4 bg-white border border-blue-500 text-blue-600 px-4 py-2 rounded hover:bg-blue-50 transition" %>
<% end %>

<%# Response button — only for volunteers who haven't responded yet %>
<% if user_signed_in? && current_user != @request.user && !@request.responses.exists?(user_id: current_user.id) %>
  <%= button_to 'Відгукнутися', request_responses_path(@request), method: :post,
                class: 'bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 mt-4' %>
<% end %>

<%= link_to 'Назад', requests_path, class: 'text-blue-600 underline mt-4 inline-block' %>
